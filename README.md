# autocomplete

To Run this application open command prompt, navigate to the folder where the project is downladed.

Enter mvn spring-boot:run

Open browser and enter http://localhost:8084/swagger-ui.html to test the API's
